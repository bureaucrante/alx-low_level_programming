#include <stdio.h>

int main(void)
{

	char ASCII;
	printf("Please enter a character: ");
	scanf("%c", &ASCII);

	printf("%i\n", ASCII);
	return (0);

}
//this illustrates that a char character can simply be converted into an integer by changing the conversion key
